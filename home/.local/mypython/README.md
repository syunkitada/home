# Python Tools

## セットアップ方法

- makeを実行すると、内部の[scripts/setup_common.sh](/scripts/setup_common.sh)によってpython_toolsがインストールされ使えるようになります。
- PATHの設定は、[home/.envrc](/home/.envrc) で管理されており、`${HOME}/home/python_tools/.venv/bin`が利用できます。
